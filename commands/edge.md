---
description: Edge — tease-and-deny game
---

Call `start_game` with type `edge` on the active target. It ramps to the brink, holds, then
cuts to nothing and rests, with the peak creeping higher each round. Tell the user it's running
and they can say **softer**, **harder**, or **safeword**. Match the user's language.
