---
description: Turn it up — raise intensity
---

Call `list_devices` to read the current intensity of the active device(s), then `vibrate`
the target (`all` unless one is selected) at `min(1.0, current + 0.2)`. Reply in one short line.
Match the user's language.
