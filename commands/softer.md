---
description: Ease off — lower intensity
---

Call `list_devices` to read the current intensity, then `vibrate` the target at
`max(0, current - 0.2)`. Reply in one short line. Match the user's language.
