---
description: Surprise me — pick a random mode
---

Pick one at random and start it on the active target: either a `start_game`
(`roulette` | `ambient` | `edge` | `wheel`) or a `pattern`
(`pulse` | `wave` | `escalate` | `tease` | `heartbeat` | `staircase` | `earthquake`, loops 3).
Don't tell them which until it's running. One teasing line back. Match the user's language.
